<% layout('layouts/boilerplate'); %>
<div class="page">
    <h2 class="text-center mb-5">Atraktivne <span class="text-success">Destinacije</span></h2>
    <div class="container d-flex flex-wrap justify-content-center gap-4">
        <% for( let item of pageData.aktivnosti ) { %>
            <a class="text-decoration-none" href="/showAktivnost/<%= item.id %>"> 
                <div class="card" data-aos="fade-up">              
                    <div class="card-img">
                        <img src="<%= item.imgsm %>" class="" alt="<%= item.title %>">     
                    </div>   
                    <div class="card-body text-center bg-success">
                        <span class="card-title m-0 text-white fs-6" ><%= item.title %></span>
                    </div> 
                </div>
            </a>
        <% } %>
        
        
    </div>   
</div>